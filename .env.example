# ── Django (web service) ──
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# ── FastAPI (api service) ──
# API anahtarlari istemciden gelir; sunucuda zorunlu degildir.
CORS_ALLOW_ORIGINS=http://127.0.0.1:5500,http://localhost:5500
# CHROMA_PERSIST_DIR=/var/data/chroma

# ── Django -> FastAPI export koprusu ──
# FASTAPI_BASE_URL=http://127.0.0.1:8001

# ── Opsiyonel entegrasyon testleri (pytest -m integration) ──
# TEST_GEMINI_API_KEY=
# JIRA_BASE_URL=
# JIRA_EMAIL=
# JIRA_API_TOKEN=
# JIRA_PROJECT_KEY=
