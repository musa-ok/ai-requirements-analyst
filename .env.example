# Lokal gelistirme: kopyalayin -> cp .env.example .env
# .env dosyasi git'e gitmez.

# ── Django ──
SECRET_KEY=dev-icin-rastgele-bir-deger-yazin
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# ── Django -> FastAPI export koprusu ──
FASTAPI_BASE_URL=http://127.0.0.1:8001

# ── FastAPI (api servisi) ──
CORS_ALLOW_ORIGINS=http://127.0.0.1:8000,http://127.0.0.1:5500

# ── Opsiyonel entegrasyon testleri ──
# TEST_GEMINI_API_KEY=
